# Portal Services API - Docker Environment Variables
# Este arquivo contém as variáveis de ambiente para o ambiente Docker

# Server Configuration
NODE_ENV=production
PORT=3001
APP_VERSION=2.0.0
FEATURES=email-management,service-management,quotations

# Database Configuration (Docker)
DB_HOST=db
DB_PORT=5432
DB_NAME=portalservicesdb
DB_USER=admin
DB_PASSWORD=admin
DB_SSL=false

# API Configuration
API_URL=http://localhost:3001

# Logging
LOG_LEVEL=info

# Docker specific settings
HOST=0.0.0.0
DOCKER_ENV=true

# Health Check Configuration
HEALTH_CHECK_TIMEOUT=5000
HEALTH_CHECK_INTERVAL=30000

# Application Settings
MAX_REQUEST_SIZE=10mb
REQUEST_TIMEOUT=30000

# Security (for production, change these values)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
SESSION_SECRET=your-session-secret-change-in-production

# Email Configuration (optional - configure if using email features)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=your-email@gmail.com

# Google APIs (optional - configure if using Google services)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback
