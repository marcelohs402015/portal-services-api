# Portal Services API - Render Blueprint com PostgreSQL Separado
# Deploy: https://render.com/deploy?repo=https://github.com/SEU_USUARIO/portal-services-api

services:
  # Portal Services API usando PostgreSQL separado no Render
  - type: web
    name: portal-services-api
    plan: free
    region: oregon
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    healthCheckPath: /health
    numInstances: 1
    
    # Variáveis de ambiente
    envVars:
      # Server Configuration
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: APP_VERSION
        value: "2.0.0"
      
      # Database Configuration (PostgreSQL Separado no Render)
      - key: DATABASE_URL
        value: postgresql://postgres_db_h43p_user:ZUHkKGgeWy3Sdn9vd1sbQPrl05QwUdW6@dpg-d360h71r0fns73bhcbjg-a.oregon-postgres.render.com:5432/postgres_db_h43p
      
      # API Key Configuration
      - key: API_KEY_SECRET
        value: portal-services-api-key-secret-2024
      
      # API Configuration
      - key: LOG_LEVEL
        value: info
      - key: CORS_ORIGIN
        value: "*"
    
    # Build Configuration
    buildCommand: npm run build
    startCommand: npm start
    
    # Health Check
    healthCheckPath: /health
    healthCheckGracePeriod: 180