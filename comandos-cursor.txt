# 🚀 Comandos para Terminal do Cursor - Portal Services

## PASSO 1: PREPARAR PROJETO LOCAL
# Execute estes comandos no terminal do Cursor (Ctrl + `)

# 1.1 Instalar dependências do backend
cd appserver
npm install

# 1.2 Instalar dependências do frontend (se houver)
cd ../appclient
npm install
cd ..

## PASSO 2: CONFIGURAR BANCO DE DADOS LOCAL
# Usar Docker Compose para PostgreSQL

# 2.1 Subir banco de dados
npm run dev

# 2.2 Verificar se está rodando
npm run dev:logs

## PASSO 3: CONFIGURAR VARIÁVEIS DE AMBIENTE
# Criar arquivo .env baseado em env.dev

## PASSO 4: TESTAR LOCAL
# 4.1 Backend
npm run server:dev

# 4.2 Frontend (se houver)
npm run client:dev

# 4.3 Ambos juntos
npm run dev

## COMANDOS ÚTEIS PARA DESENVOLVIMENTO

# Ver logs do Docker
docker-compose logs -f

# Parar serviços
npm run dev:stop

# Resetar banco de dados
npm run db:reset

# Executar seeds
npm run db:seed

## COMANDOS GIT
git add .
git commit -m "feat: descrição da feature"
git push origin main

## COMANDOS DEBUG LOCAL
# Testar health check
curl http://localhost:3001/health

# Ver logs do servidor
npm run dev:logs

# Conectar ao banco via psql
docker exec -it portal-services-db psql -U admin -d portalservicesdb

## COMANDOS PARA BUILD
# Build do servidor
npm run build:server

# Build completo
npm run build
