# 🚀 Comandos para Terminal do Cursor - Portal Services

## PASSO 1: PREPARAR PROJETO
# Execute estes comandos no terminal do Cursor (Ctrl + `)

# 1.1 Executar setup automático
./setup-supabase.sh

# 1.2 Instalar dependências (se não executou o script)
npm install @supabase/supabase-js
cd ..

# 1.3 Instalar Render CLI (opcional)
npm install -g @render/cli

## PASSO 2: CONFIGURAR SUPABASE
# (Feito no browser - supabase.com)
# - Criar projeto: portal-services  
# - Senha: Admin@123#2025
# - Executar SQL: supabase-schema.sql

## PASSO 3: CONFIGURAR VARIÁVEIS
# (Use o editor do Cursor para criar/editar)

## PASSO 4: TESTAR LOCAL
npm start

## PASSO 5: DEPLOY RENDER
# 5.1 Login (opcional - pode usar interface web)
render login

# 5.2 Deploy via interface web do Render
# - Acesse localhost:3001
# - Conecte repositório GitHub
# - Configure build settings

# 5.3 Configurar env vars no Render (via interface web)
# - REACT_APP_SUPABASE_URL
# - REACT_APP_SUPABASE_ANON_KEY

## COMANDOS ÚTEIS PARA MONITORAMENTO

# Ver logs do Render (via interface web)
# - Acesse localhost:3001/dashboard
# - Selecione seu serviço
# - Vá em "Logs"

# Ver informações do projeto (via interface web)
# - Dashboard do Render
# - Seção "Services"

## COMANDOS GIT (se usar integração GitHub)
git add .
git commit -m "feat: deploy to Supabase + Render"
git push origin main

## COMANDOS DEBUG LOCAL
# Testar conexão Supabase
npm start
# Abrir: http://localhost:3000

# Ver logs do React
# (logs aparecem automaticamente no terminal)

## COMANDOS PARA ATUALIZAR APP
# Após fazer mudanças no código:
git push origin main
# (Render faz deploy automático se configurado)
